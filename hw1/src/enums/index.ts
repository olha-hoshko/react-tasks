export enum RequestTypes {
  posts = 'posts',
  comments = 'comments',
  albums = 'albums',
  photos = 'photos',
  todos = 'todos',
  users = 'users',
};

export enum ThemeModes {
  dark = 'dark', 
  light = 'light',
};